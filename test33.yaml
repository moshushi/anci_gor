---
- name: get ip from ansible hosts playbook
  # hosts: '{{ myhosts }}'
  hosts: h-90
  # hosts: h-36
  gather_facts: yes
  become: true

  tasks:
    # - name: Show variable
    #   debug:
    #     var: hostvars

    # - name: Show variable
    #   debug:
    #     var: hostvars[inventory_hostname]

    # - name: Show variable
    #   debug:
    #     var: hostvars[inventory_hostname]['ansible_host']

    - name: Show variable inventory_hostname from host file
      debug:
        msg: "{{ hostvars[inventory_hostname]['inventory_hostname'] }}"

    - name: get ip from hosts-file ansible
      debug:
        msg: "{{ hostvars[inventory_hostname]['ansible_host'] }}"

    # - name: get ip from hosts-file ansible
    #   debug:
    #     msg: "{{ hostvars[default] }}"
